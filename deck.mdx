import { Split, themes } from 'mdx-deck';

export const theme = {
//  ...themes.aspect,
  ...themes.swiss,
  ...themes.prism,
}

# Velkommen til front-end workshop med React og Typescript!

<Split>
  <img src='https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg' />
  <img style={{transform: "scale(60%)"}} src='https://upload.wikimedia.org/wikipedia/commons/4/4c/Typescript_logo_2020.svg' />
</Split>

---

## Først
```bash
npx create-react-app \
    frontend-workshop \
    --template typescript
```
---

## Destrukturering
```typescript
const [a, b] = [1, 2];
console.log(a); // 1
console.log(b); // 2

const [first, ...rest] = ["a", "b", "c", "d"];
console.log(first); // "a"
console.log(rest);  // ["b", "c", "d"]
```

---

## Javascript-objekter
```typescript
const person = {
  name: "Magnus",
  age: 29,
  makeSound: () => console.log("Jasså!")
};

console.log(person.name);    // "Magnus"
console.log(person["name"]); // "Magnus"
```

---

## Destrukturering av objekter
```typescript
const { message, ...otherStuff } = {
  date: "21.04.21",
  message: "Velkommen til workshop!"
};

console.log(message);    // "Velkommen til workshop!"
console.log(otherStuff); // { date: "21.04.21" }
```

---

## Spread
```typescript
const numbers = [1, 2, 3];

console.log(numbers);    // [1, 2, 3]
console.log(...numbers); // 1, 2, 3

console.log([0, ...numbers, 4]); // [0, 1, 2, 3, 4]
```

---

## Spread
```typescript
const something = {
  x: 3,
  y: "Hei",
};
const other = {
  y: "Hallo",
  z: [1, 2],
};
const merged = { ...something, ...other };
//           = { x: 3, y: "Hei", y: "Hallo", z: [1, 2] }

console.log(merged);
// {
//   x: 3,
//   y: "Hallo",
//   z: [1, 2]
// }
```

---

## Typer og inferens

```typescript
const x: boolean = true;

let y = 5;
y = "Jeg vil være en streng!";
// Type 'string' is not assignable to type 'number'.

let z: number | string = 5;
z = "Dette er ok 👍";
```

---

## Typedeklarasjoner
```typescript
type Foo = {
  bar: number[],
  baz: string
}

const foo: Foo = { bar: [9], baz: "Hei" };

const foo2: Foo = { bar: [9], baz: 1 }
// Type 'number' is not assignable to type 'string'.
```

---

## Funksjoner
```typescript
function add(a: number, b:number): number {
  return a + b;
}

const sub = (a: number, b: number) => a - b;
```

---

## Livekoding
```bash
cd frontend-workshop
yarn start

# Om du ikke har yarn installert:
npm start
```

---

## Takk for oss
<Split style={{ alignItems: "start" }}>
  <div>
    <h3>Magnus Myhrvold Bratteng</h3>
    <ul style={{ listStyle: "none", padding: 0 }}>
      <li>
        <img
          style={{ clipPath: "circle()", maxWidth: "30%" }}
          src="https://avatars.githubusercontent.com/u/56268716"
          alt="Magnus Myhrvold Bratteng"
        />
      </li>
      <li>
        <a href="mailto:magnumb@ifi.uio.no">magnumb@ifi.uio.no</a>
      </li>
      <li>
        <a href="https://github.com/magnusmb">github.com/magnusmb</a>
      </li>
    </ul>
  </div>
  <div>
    <h3>Bjørn-Andreas Lamo</h3>
    <ul style={{ listStyle: "none", padding: 0 }}>
      <li>
        <img
          style={{ clipPath: "circle()", maxWidth: "30%" }}
          src="https://media.discordapp.net/attachments/813540167473561653/834115762347180137/IMG_0884.jpg"
          alt="Bjørn-Andreas Lamo"
        />
      </li>
      <li>
        <a href="mailto:janilam@uio.no">janilam@uio.no</a>
      </li>
    </ul>
  </div>
</Split>
